<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Captured job-card photos live in filesDir/job_photos/ -->
    <files-path
        name="job_photos"
        path="job_photos/" />

    <!-- Generic cache path (used by other parts of the app if needed) -->
    <cache-path
        name="cache"
        path="/" />

</paths>